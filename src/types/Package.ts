export interface Package {
    name: string,
    total_price: number,
    image: string,
    is_active: boolean,
    summary: string,
    creation_date: Date,
    description:string,
    score:string,
}
